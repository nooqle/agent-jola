{
  "name": "@agent-poppy/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm --filter @agent-poppy/replay build && pnpm --filter @agent-poppy/protocol build && tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "pnpm --filter @agent-poppy/replay build && pnpm --filter @agent-poppy/protocol build && vitest run src"
  },
  "dependencies": {
    "@agent-poppy/agent": "workspace:*",
    "@agent-poppy/core": "workspace:*",
    "@agent-poppy/protocol": "workspace:*",
    "@agent-poppy/replay": "workspace:*",
    "@agent-poppy/strategy": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.3.3",
    "nanoid": "^5.1.5",
    "zod": "^3.25.7"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
